# BrainSelector Homebrew tap

Install the signed and notarized BrainSelector app and terminal dashboard:

```sh
brew install --cask jniedzie/brainselector/brainselector
```

Upgrade later with:

```sh
brew upgrade --cask brainselector
```

Release files and checksums are published at https://github.com/jniedzie/brainselector/releases.
